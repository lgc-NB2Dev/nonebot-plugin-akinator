<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  {% block style -%}
  <style>
    :root {
      --color: #18455a;
    }

    @font-face {
      font-family: Icon;
      font-style: normal;
      font-weight: 400;
      src: url('/res/icon.woff2') format('woff2');
    }

    span.f-icon {
      font-family: Icon;
      font-weight: normal;
      font-style: normal;
      font-size: 24px;
      line-height: 1;
      letter-spacing: normal;
      text-transform: none;
      display: inline-block;
      white-space: nowrap;
      word-wrap: normal;
      direction: ltr;
      -webkit-font-feature-settings: 'liga';
      font-feature-settings: 'liga';
      -webkit-font-smoothing: antialiased;
    }

    main {
      width: 540px;
      background: url('/res/bg.png') no-repeat center/cover;
      color: var(--color);
      font-family: sans-serif;
    }

    .bg-mask-wrapper {
      width: 100%;
      height: 100%;
      background-color: rgba(255, 255, 255, 0.36);
    }

    /* shared */

    div.diamond {
      content: '';
      display: block;
      width: 8px;
      height: 8px;
      background-color: rgba(255, 255, 255, 0.5);
      clip-path: polygon(50% 0, 100% 50%, 50% 100%, 0 50%);
    }

    .tb-border-box {
      border-top: 3px solid white;
      border-bottom: 3px solid white;
      background-color: #d2ddea;
    }

    .question-box-tail {
      width: 8px;
      height: 16px;
      clip-path: polygon(100% 0, 0 50%, 100% 100%);
    }
  </style>
  {%- endblock %}
  {{ additional_style | safe }}
</head>

<body>
  <main>
    <div class="bg-mask-wrapper">
      {% block main %}{% endblock %}
    </div>
  </main>
</body>

{% block extra %}{% endblock %}

</html>
